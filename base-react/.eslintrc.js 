module.exports =  {
  parser:  '@babel/eslint-parser',  // Specifies the ESLint parser
  extends:  [
    'react-app',  // Uses the recommended rules from @eslint-config-react-app
    'react-app/jest',  // Uses the recommended rules from @eslint-config-react-app/jest
    'plugin:jsx-a11y/recommended',  // Uses the recommended rules from @eslint-config-react-app/jsx-a11y
  ],
  parserOptions:  {
    ecmaVersion:  2018,  // Allows for the parsing of modern ECMAScript features
    sourceType:  'module',  // Allows for the use of imports
    ecmaFeatures:  {
      jsx:  true,  // Allows for the parsing of JSX
    },
  },
  rules:  {
    // Place to specify ESLint rules. Can be used to overwrite rules specified from the extended configs
    // e.g. '@typescript-eslint/explicit-function-return-type': 'off',
  },
  settings:  {
    react:  {
      version:  'detect',  // Tells eslint-plugin-react to automatically detect the version of React to use
    },
  },
};